export default {
	// getData:function (state) {
		// console.log(state.areas,'---');
		// let vm = new Vue();
		// vm.$$api_projectControl_areaTree({
		// 	data     : {},
		// 	fn       : data => {
		// 		state.areas = data//地区数据
		// 		console.log(data);
		// 	},
		// 	errFn    : (err) => {
		// 		console.log(err);
		// 	}
		// });
		// return state.areas;

		// axios.post(url, params)
		// 	.then(response => {
		// 		alert('Api--ok');
		// 		resolve(response.data);
		// 	})
		// 	.catch((error) => {
		// 		console.log(error)
		// 		reject(error)
		// 	})
	// }
	// getLang: (state) => {
	// 	return state.lang;
	// },
	/*getAreaByParentId:  (state, getters) => (parent_id)  => {
		return state.areas.filter(area => area.parent_id === parent_id);
	},*/
};
